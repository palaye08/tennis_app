spring:
  application:
    name: tennis-backend
  datasource:
    url: jdbc:mysql://localhost:3306/tennis_db?createDatabaseIfNotExist=true
    username: root
    password: passer123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
